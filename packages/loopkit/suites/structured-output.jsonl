# JSON-to-schema structured output (Phase 1). Each task carries its own
# "schema"; scored by evals.validate_schema via the json_schema match mode.
{"id": "so-1", "prompt": "Summarize this event as JSON with fields: title (string), date (string), attendees (integer). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nEvent: The Q3 planning meeting is on 2026-08-04 and 12 people are attending.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["title", "date", "attendees"], "properties": {"title": {"type": "string"}, "date": {"type": "string"}, "attendees": {"type": "integer"}}}}
{"id": "so-2", "prompt": "Represent this support ticket as JSON with fields: ticket_id (string), priority (one of low/medium/high/critical), resolved (boolean). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nTicket TCK-4471 is critical priority and has not been resolved yet.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["ticket_id", "priority", "resolved"], "properties": {"ticket_id": {"type": "string"}, "priority": {"type": "string", "enum": ["low", "medium", "high", "critical"]}, "resolved": {"type": "boolean"}}}}
{"id": "so-3", "prompt": "Represent this weather report as JSON with fields: city (string), temp_c (number), conditions (string). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nIt's 18.5 degrees Celsius and partly cloudy in Denver right now.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["city", "temp_c", "conditions"], "properties": {"city": {"type": "string"}, "temp_c": {"type": "number"}, "conditions": {"type": "string"}}}}
{"id": "so-4", "prompt": "Represent this recipe as JSON with fields: name (string), servings (integer), ingredients (array of strings). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nSimple Tomato Soup serves 4 and needs tomatoes, onion, garlic, and vegetable broth.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["name", "servings", "ingredients"], "properties": {"name": {"type": "string"}, "servings": {"type": "integer"}, "ingredients": {"type": "array", "items": {"type": "string"}}}}}
{"id": "so-5", "prompt": "Represent this product as JSON with fields: sku (string), price (number), in_stock (boolean). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nSKU WM-2210 is priced at $24.99 and is currently in stock.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["sku", "price", "in_stock"], "properties": {"sku": {"type": "string"}, "price": {"type": "number"}, "in_stock": {"type": "boolean"}}}}
{"id": "so-6", "prompt": "Represent this person as JSON with fields: name (object with first and last strings), age (integer). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nAlex Rivera is 34 years old.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["name", "age"], "properties": {"name": {"type": "object", "required": ["first", "last"], "properties": {"first": {"type": "string"}, "last": {"type": "string"}}}, "age": {"type": "integer"}}}}
{"id": "so-7", "prompt": "Represent this task list as JSON with a single field tasks (array of objects, each with id integer and done boolean). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nTask 1 is done, task 2 is not done, task 3 is done.", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["tasks"], "properties": {"tasks": {"type": "array", "items": {"type": "object", "required": ["id", "done"], "properties": {"id": {"type": "integer"}, "done": {"type": "boolean"}}}}}}}
{"id": "so-8", "prompt": "Analyze the sentiment of this review as JSON with fields: sentiment (one of positive/negative/neutral), confidence (number between 0 and 1). Reply with a fenced ```json block, then repeat the compact JSON after an ANSWER: line.\n\nReview: \"Absolutely loved this product, works perfectly and arrived early!\"", "expected": "", "match": "json_schema", "schema": {"type": "object", "required": ["sentiment", "confidence"], "properties": {"sentiment": {"type": "string", "enum": ["positive", "negative", "neutral"]}, "confidence": {"type": "number"}}}}
