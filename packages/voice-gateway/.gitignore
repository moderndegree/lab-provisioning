__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Test fixture: the Silero VAD graph. 2.3 MB of binary that the role already
# fetches with a checksum (roles/voice/defaults/main.yml) — no reason for it to
# live in git twice. tests/conftest.py skips itself when it is absent and says
# where to get it.
tests/silero_vad.onnx
