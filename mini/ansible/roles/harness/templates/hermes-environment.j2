# Hermes Agent runtime secrets — managed by Ansible (ansible-vault sourced)
# File permissions: root:{{ node_user }} 0640
# Sourced by hermes-gateway.service via EnvironmentFile=

# LLM provider: configure via `hermes config` after provisioning (e.g. local Ollama)
