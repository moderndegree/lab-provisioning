---
# Reloading alone only affects devices that appear afterwards; /dev/kfd and the
# render nodes already exist, so they are retriggered in the same handler to
# pick up the new modes without a reboot.
- name: reload udev rules
  ansible.builtin.shell:
    cmd: udevadm control --reload-rules && udevadm trigger
  changed_when: true
