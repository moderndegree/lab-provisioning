# {{ ansible_managed }}
# Root-only (0600). Source this to run restic by hand:
#   sudo bash -c 'source /etc/restic/restic.env && restic snapshots'
export RESTIC_REPOSITORY="{{ restic_repo }}"
export RESTIC_PASSWORD="{{ restic_password }}"
