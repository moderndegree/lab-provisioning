# Podman bridge network for Open WebUI (Ansible-managed, do not edit by hand).
# DNS search domain (Tailscale MagicDNS resolution, e.g. "mini" → mini.{{ tailnet_dns_search }})
# is a per-container key (DNSSearch=), not a network one — set on openwebui.container.j2 instead.
[Network]
