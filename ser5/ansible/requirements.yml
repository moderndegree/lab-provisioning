---
# Ansible collection dependencies for ser5 provisioning.
# Install once with: make install-deps
collections:
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.libvirt
    version: ">=1.3.0"
