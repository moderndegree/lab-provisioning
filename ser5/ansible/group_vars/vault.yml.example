---
# Ansible Vault secret stubs.
# This file (vault.yml.example) is tracked with placeholder values.
# vault.yml (the real file with secrets) is gitignored.
#
# ─── First-time setup ─────────────────────────────────────────────────────────
#   1. make init           → copies this file to ansible/group_vars/vault.yml
#   2. Edit vault.yml      → fill in real values (see generation commands below)
#   3. make vault-encrypt  → encrypts vault.yml in-place with ansible-vault
#   4. Commit encrypted vault.yml to your private fork/clone
#
# ─── Generating values ────────────────────────────────────────────────────────
# Password hash (must match PASSWORD_HASH in .bootstrap.env):
#   python3 -c "import crypt, getpass; print(crypt.crypt(getpass.getpass(), crypt.mksalt(crypt.METHOD_SHA512)))"
#   OR: mkpasswd --method=sha-512    (apt install whois)
#
# Tailscale auth key:
#   https://login.tailscale.com/admin/settings/keys
#   Use an ephemeral key tagged for ser5.

# SHA-512 crypt hash of the primary user's password.
# Must match PASSWORD_HASH in .bootstrap.env so autoinstall and Ansible are consistent.
vault_user_password_hash: "PLACEHOLDER_SHA512_CRYPT_HASH"

# Tailscale ephemeral auth key (from Tailscale admin console → Settings → Keys)
vault_tailscale_authkey: "tskey-auth-PLACEHOLDER"

# Wi-Fi WPA2 passphrase for the SSID set in all.yml (wifi_ssid).
# Leave as-is / empty if the host is wired-only.
vault_wifi_psk: "PLACEHOLDER_WIFI_PASSWORD"

# Cloudflare Tunnel token (from Zero Trust dashboard → Networks → Tunnels)
# The cloudflared role only starts the tunnel when this is non-empty and
# does not begin with "PLACEHOLDER" — safe to leave as-is until you need it.
vault_cloudflared_token: "PLACEHOLDER_CLOUDFLARE_TUNNEL_TOKEN"

# restic repository password (only needed when enable_backups: true).
# Generate:  openssl rand -base64 24   — and store a copy OFF this machine;
# losing it makes every snapshot unrecoverable.
vault_restic_password: "PLACEHOLDER_RESTIC_PASSWORD"

# Optional xAI API key (console.x.ai) for Hermes provider:xai and/or Grok Build
# pay-as-you-go fallback. This deployment uses SuperGrok subscription OAuth
# instead (`hermes auth add xai-oauth` / `grok login`, run manually on the
# box — Ansible can't automate an interactive OAuth login), so
# enable_hermes_xai_api_key stays false and this is left as a placeholder.
# Only written into HERMES_HOME/.env when enable_hermes_xai_api_key: true and
# this value is non-empty and does not begin with PLACEHOLDER.
vault_xai_api_key: "PLACEHOLDER_XAI_API_KEY"

# Telegram is not managed by Ansible — set up manually on the box (bot token
# + `hermes pairing approve telegram <code>`) if/when wanted.

# Hermes Discord bot token, from the Discord Developer Portal → Bot tab.
# Safe to leave as PLACEHOLDER_*: the role only writes this into HERMES_HOME/.env
# when enable_hermes_messaging and enable_hermes_discord are both true and the
# value is non-empty and does not begin with PLACEHOLDER.
vault_hermes_discord_bot_token: "PLACEHOLDER_DISCORD_BOT_TOKEN"
