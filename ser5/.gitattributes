# Normalise all text files to LF in the repo; checkout with LF on all platforms.
# This ensures Makefile recipe tabs, bash scripts, and YAML files work correctly
# when checked out on Linux (the Ansible control node / ser5 target).
* text=auto eol=lf
